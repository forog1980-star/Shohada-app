"use strict";

const AllMartyrsUI = (() => {
  const TYPE_LABELS = { martyr: "شهید", unknown_martyr: "شهید گمنام", non_martyr: "غیرشهید — اموات" };
  const MONTHS = ["فروردین", "اردیبهشت", "خرداد", "تیر", "مرداد", "شهریور", "مهر", "آبان", "آذر", "دی", "بهمن", "اسفند"];
  function escapeHtml(value) { return String(value ?? "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#039;"); }
  function faDigits(value) { return String(value ?? "").replace(/[0-9]/g, d => "۰۱۲۳۴۵۶۷۸۹"[Number(d)]); }
  function dateDisplay(date) { if (!date) return "—"; const d = date.day || "", m = date.month_name || date.month || "", y = date.year || ""; return [d, m, y].filter(Boolean).map(faDigits).join(" / ") || "—"; }
  function pieceLabel(record) { if (record.source === "outside") return "قطعات عمومی بهشت زهرا"; return record.grave_piece ? `قطعه ${faDigits(record.grave_piece)}` : "—"; }
  function graveHtml(record) { return `<div class="all-martyrs-grave"><strong>مزار شهید</strong><span>${escapeHtml(pieceLabel(record))}</span><span>ردیف ${escapeHtml(faDigits(record.grave_row || "—"))}</span><span>شماره ${escapeHtml(faDigits(record.grave_number || "—"))}</span></div>`; }
  function card(record) { const fullName = [record.first_name, record.last_name].filter(Boolean).join(" ") || TYPE_LABELS[record.record_type]; return `<article class="all-martyrs-card" data-record-id="${escapeHtml(record.id)}"><div class="all-martyrs-card-head"><div><small class="all-martyrs-kicker">${escapeHtml(TYPE_LABELS[record.record_type])}</small><h3>${escapeHtml(fullName)}</h3></div><span class="all-martyrs-source">${record.source === "outside" ? "عمومی" : "گلزار"}</span></div><div class="all-martyrs-card-core"><div><span>نام پدر</span><strong>${escapeHtml(record.father_name || "—")}</strong></div></div>${graveHtml(record)}<button type="button" class="all-martyrs-detail-button" data-action="detail" data-record-id="${escapeHtml(record.id)}">مشاهده اطلاعات کامل</button></article>`; }
  function render(container, records, total, options = {}) { const canExport = options.canExport !== false; container.innerHTML = `<div class="all-martyrs-result-summary"><div><strong>نتایج جستجو</strong><span>${faDigits(Number(total).toLocaleString("en-US"))} مورد</span></div>${canExport && records.length ? '<button type="button" class="all-martyrs-export" data-action="export">📥 خروجی Excel</button>' : ""}</div><div class="all-martyrs-results">${records.length ? records.map(card).join("") : '<div class="all-martyrs-empty">نتیجه‌ای پیدا نشد.</div>'}</div>`; }
  function detail(container, record) { const fullName = [record.first_name, record.last_name].filter(Boolean).join(" ") || TYPE_LABELS[record.record_type]; const edited = record.edited_at ? `<div class="all-martyrs-edited-meta">آخرین ویرایش: ${escapeHtml(record.edited_at)}</div>` : ""; container.innerHTML = `<section class="all-martyrs-detail"><div class="all-martyrs-detail-head"><div><small>${escapeHtml(TYPE_LABELS[record.record_type])}</small><h2>${escapeHtml(fullName)}</h2></div><span>${escapeHtml(record.source === "outside" ? "قطعات عمومی بهشت زهرا" : "اطلاعات گلزار")}</span></div>${edited}<div class="all-martyrs-detail-grid"><div><label>نام پدر</label><strong>${escapeHtml(record.father_name || "—")}</strong></div><div><label>جنسیت</label><strong>${escapeHtml(record.gender || "—")}</strong></div><div><label>تاریخ تولد</label><strong>${escapeHtml(dateDisplay(record.birth_date))}</strong></div><div><label>تاریخ شهادت</label><strong>${escapeHtml(dateDisplay(record.death_date))}</strong></div><div><label>سن</label><strong>${escapeHtml(faDigits(record.age_display || "—"))}</strong></div><div><label>عملیات</label><strong>${escapeHtml(record.martyrdom_operation || "—")}</strong></div><div><label>دسته‌بندی شهادت</label><strong>${escapeHtml(record.martyrdom_category || "—")}</strong></div><div><label>منطقه / محل شهادت</label><strong>${escapeHtml(record.martyrdom_location || "—")}</strong></div><div class="detail-grave">${graveHtml(record)}</div>${record.description ? `<div class="detail-description"><label>توضیحات</label><p>${escapeHtml(record.description)}</p></div>` : ""}</div><div class="all-martyrs-detail-actions"><button type="button" class="all-martyrs-back" data-action="back">بازگشت به نتایج</button><button type="button" class="all-martyrs-edit" data-action="edit" data-record-id="${escapeHtml(record.id)}">ویرایش اطلاعات</button></div></section>`; }
  function edit(container, record) { const fullName = [record.first_name, record.last_name].filter(Boolean).join(" "); const input = (key, label, value, type = "text") => `<label><span>${label}</span><input data-edit-field="${key}" type="${type}" value="${escapeHtml(value)}"></label>`; container.innerHTML = `<section class="all-martyrs-detail"><div class="all-martyrs-detail-head"><div><small>ویرایش اطلاعات</small><h2>${escapeHtml(fullName || TYPE_LABELS[record.record_type])}</h2></div></div><p class="all-martyrs-edit-note">ویرایش در این مرحله فقط در نشست جاری مرورگر نگه‌داری می‌شود؛ منبع Excel و Supabase بدون اجازه شما تغییر نمی‌کند.</p><div class="all-martyrs-edit-grid">${input("first_name", "نام", record.first_name)}${input("last_name", "نام خانوادگی", record.last_name)}${input("father_name", "نام پدر", record.father_name)}${input("gender", "جنسیت", record.gender)}${input("birth_day", "روز تولد", record.birth_date.day)}${input("birth_month", "ماه تولد", record.birth_date.month_name || record.birth_date.month)}${input("birth_year", "سال تولد", record.birth_date.year)}${input("death_day", "روز شهادت", record.death_date.day)}${input("death_month", "ماه شهادت", record.death_date.month_name || record.death_date.month)}${input("death_year", "سال شهادت", record.death_date.year)}${input("martyrdom_operation", "عملیات", record.martyrdom_operation)}${input("martyrdom_category", "دسته‌بندی شهادت", record.martyrdom_category)}${input("martyrdom_location", "منطقه / محل شهادت", record.martyrdom_location)}${input("grave_piece", "قطعه", record.grave_piece)}${input("grave_row", "ردیف", record.grave_row)}${input("grave_number", "شماره مزار", record.grave_number)}</div><div class="all-martyrs-detail-actions"><button type="button" class="all-martyrs-back" data-action="back">انصراف و بازگشت</button><button type="button" class="all-martyrs-edit" data-action="save-edit" data-record-id="${escapeHtml(record.id)}">ذخیره موقت</button></div></section>`; }
  function ensureScrollTopControl() {
    if (document.getElementById("all-martyrs-scroll-top")) return;
    const style = document.createElement("style");
    style.id = "all-martyrs-scroll-top-style";
    style.textContent = ".all-martyrs-scroll-top{position:fixed;right:16px;bottom:18px;z-index:1000;width:46px;height:46px;border:1px solid #cfe0d6;border-radius:50%;background:#fff;color:#17633d;box-shadow:0 5px 18px rgba(24,49,39,.16);font:inherit;font-size:22px;font-weight:bold;line-height:1;cursor:pointer;display:grid;place-items:center;opacity:0;visibility:hidden;transform:translateY(8px);transition:opacity .18s ease,visibility .18s ease,transform .18s ease}.all-martyrs-scroll-top.is-visible{opacity:1;visibility:visible;transform:translateY(0)}.all-martyrs-scroll-top:focus-visible{outline:3px solid rgba(23,99,61,.22);outline-offset:2px}@media(max-width:760px){.all-martyrs-scroll-top{right:12px;bottom:calc(14px + env(safe-area-inset-bottom));width:44px;height:44px;font-size:21px}}";
    document.head.appendChild(style);
    const button = document.createElement("button");
    button.id = "all-martyrs-scroll-top";
    button.className = "all-martyrs-scroll-top";
    button.type = "button";
    button.setAttribute("aria-label", "بازگشت به ابتدای صفحه");
    button.title = "بازگشت به ابتدای صفحه";
    button.textContent = "↑";
    button.addEventListener("click", () => window.scrollTo({ top: 0, behavior: "smooth" }));
    document.body.appendChild(button);
    const updateVisibility = () => button.classList.toggle("is-visible", window.scrollY > 420);
    window.addEventListener("scroll", updateVisibility, { passive: true });
    updateVisibility();
  }
  ensureScrollTopControl();
  return { render, card, detail, edit, TYPE_LABELS, MONTHS, faDigits };
})();
