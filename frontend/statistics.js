"use strict";

const SUPABASE_URL = "https://bafrksgdcmglahyrppfy.supabase.co";
const SUPABASE_PUBLISHABLE_KEY = "sb_publishable_O5CkSuivysXJf-8hu1IUCA_izu8hWiX";
const TABLE_NAME = "martyrs";
const TOTAL_GRAVES = 26097;
const PIECES = [["17",103],["24",6100],["26",4514],["27",3177],["28",3523],["29",2743],["40",2948],["53",3092]];
const STAGES = {"ترمیمی":["طرح سنگ ارسال به واحد مرمت","سنگ مرمتی آماده","نصب سنگ مرمت شده"],"تعویضی":["طرح سنگ ارسال به واحد تعویض","سنگ تعویضی آماده","نصب سنگ تعویض شده"]};
const OLD_STAGE_ALIASES={"ارسال به واحد مرمت":"طرح سنگ ارسال به واحد مرمت","نصب مرمتی شده":"نصب سنگ مرمت شده","ارسال به واحد تعویض":"طرح سنگ ارسال به واحد تعویض","تعویضی نصب شده":"نصب سنگ تعویض شده"};
function normalizeStage(v){const x=String(v??"").trim();return OLD_STAGE_ALIASES[x]||x}
function normalizePiece(v){return String(v??"").trim().replace(/[۰-۹]/g,d=>String("۰۱۲۳۴۵۶۷۸۹".indexOf(d))).replace(/[٠-٩]/g,d=>String("٠١٢٣٤٥٦٧٨٩".indexOf(d))).replace(/\.0$/ ,"")}
function fa(v){return String(v).replace(/\d/g,d=>"۰۱۲۳۴۵۶۷۸۹"[Number(d)])}
function esc(v){return String(v??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;","\"":"&quot;","'":"&#39;"}[c]))}
function pct(v,d=TOTAL_GRAVES){return d?(v/d)*100:0}
async function fetchAllRows(client){const out=[],size=1000;let from=0;while(true){const {data,error}=await client.from(TABLE_NAME).select("id,piece,stone_type,stage,status").order("id",{ascending:true}).range(from,from+size-1);if(error)throw error;const batch=data||[];out.push(...batch);if(batch.length<size)break;from+=size}const seen=new Set();return out.filter(r=>{const k=String(r.id);if(seen.has(k))return false;seen.add(k);return true})}
function emptyType(type){return{total:0,completed:0,stages:Object.fromEntries(STAGES[type].map(s=>[s,0]))}}
function buildModel(rows){const m={repair:emptyType("ترمیمی"),replacement:emptyType("تعویضی"),pieces:{},tracked:0,completed:0,remaining:0};for(const[piece,total]of PIECES)m.pieces[piece]={total,repair:Object.fromEntries(STAGES["ترمیمی"].map(s=>[s,0])),replacement:Object.fromEntries(STAGES["تعویضی"].map(s=>[s,0]))};for(const r of rows){const type=r?.stone_type;if(type!=="ترمیمی"&&type!=="تعویضی")continue;const stage=normalizeStage(r?.stage),target=type==="ترمیمی"?m.repair:m.replacement;target.total++;if(Object.prototype.hasOwnProperty.call(target.stages,stage))target.stages[stage]++;if(stage===STAGES[type][2])target.completed++;const p=normalizePiece(r?.piece);if(!m.pieces[p])continue;const pt=type==="ترمیمی"?m.pieces[p].repair:m.pieces[p].replacement;if(Object.prototype.hasOwnProperty.call(pt,stage))pt[stage]++}m.tracked=m.repair.total+m.replacement.total;m.completed=m.repair.completed+m.replacement.completed;m.remaining=Math.max(m.tracked-m.completed,0);return m}
function bar(label,value,den){const p=Math.min(pct(value,den),100);return`<div class="bar"><div class="bar-head"><span>${esc(label)}</span><strong>${fa(value)} — ${fa(p.toFixed(1))}٪</strong></div><div class="track"><div class="fill" style="width:${p.toFixed(2)}%"></div></div><small class="sub">${fa(value)} از ${fa(den)}</small></div>`}
function render(m){const app=document.getElementById("statistics-app"),overall=pct(m.completed),tracked=pct(m.tracked),remaining=m.tracked?pct(m.remaining,m.tracked):0;app.innerHTML=`<div class="page"><header class="head"><div><h1>گزارش‌های آماری</h1><p>وضعیت لحظه‌ای عملیات بهسازی سنگ مزار</p></div><button type="button" onclick="location.reload()">↻ تازه‌سازی</button></header><div class="meta">مبنای درصد از کل: <strong>${fa(TOTAL_GRAVES)}</strong> مزار در ۸ قطعه · <span class="badge">فقط خواندنی</span></div><section class="card overall"><div class="card hero metric"><span>کل مزارهای ۸ قطعه</span><strong>${fa(TOTAL_GRAVES)}</strong><small>مبنای درصد کل</small></div><div class="card metric"><span>کل موارد عملیاتی ثبت‌شده</span><strong>${fa(m.tracked)}</strong><small>${fa(tracked.toFixed(1))}٪ از کل</small></div><div class="card metric"><span>تکمیل‌شده</span><strong>${fa(m.completed)}</strong><small>${fa(overall.toFixed(1))}٪ از کل</small></div><div class="card metric"><span>باقی‌مانده عملیاتی</span><strong>${fa(m.remaining)}</strong><small>${fa(remaining.toFixed(1))}٪ از موارد عملیاتی</small></div></section><section class="card"><h2>وضعیت کلی</h2>${bar("ترمیم شده",m.repair.completed,TOTAL_GRAVES)}${bar("تعویض شده",m.replacement.completed,TOTAL_GRAVES)}${bar("تکمیل‌شده از کل",m.completed,TOTAL_GRAVES)}</section><div class="grid"><section class="card"><h2>وضعیت ترمیمی</h2><div class="stage-grid">${STAGES["ترمیمی"].map(s=>`<div class="stage"><div class="stage-title">${esc(s)}</div><strong>${fa(m.repair.stages[s])}</strong>${bar("از کل ترمیمی",m.repair.stages[s],Math.max(m.repair.total,1))}</div>`).join("")}</div></section><section class="card"><h2>وضعیت تعویضی</h2><div class="stage-grid">${STAGES["تعویضی"].map(s=>`<div class="stage"><div class="stage-title">${esc(s)}</div><strong>${fa(m.replacement.stages[s])}</strong>${bar("از کل تعویضی",m.replacement.stages[s],Math.max(m.replacement.total,1))}</div>`).join("")}</div></section></div><section class="card"><h2>وضعیت عملیات به تفکیک قطعه</h2><div class="pieces">${PIECES.map(([p,total])=>{const x=m.pieces[p];return`<article class="card piece"><h3>قطعه ${fa(p)} — ${fa(total)} مزار</h3><h4>ترمیمی</h4>${STAGES["ترمیمی"].map(s=>bar(s,x.repair[s],total)).join("")}<h4>تعویضی</h4>${STAGES["تعویضی"].map(s=>bar(s,x.replacement[s],total)).join("")}</article>`}).join("")}</div></section><div class="footer">این صفحه مستقل است و فقط داده‌های موجود در جدول <strong>martyrs</strong> را می‌خواند. هیچ ستون، رکورد یا وضعیت عملیاتی ایجاد، ویرایش یا حذف نمی‌شود.</div></div>`}
async function start(){const app=document.getElementById("statistics-app");try{if(!window.supabase?.createClient)throw new Error("کتابخانه اتصال به بانک بارگذاری نشده است.");const client=window.supabase.createClient(SUPABASE_URL,SUPABASE_PUBLISHABLE_KEY);render(buildModel(await fetchAllRows(client)))}catch(e){console.error("Independent statistics error:",e);app.innerHTML=`<div class="error"><strong>گزارش‌های آماری بارگذاری نشد.</strong><br>${esc(e.message||String(e))}<br><br>این صفحه مستقل است و اطلاعات بانک را تغییر نمی‌دهد.</div>`}}
start();
