"use strict";
(function(){
  // Stage tables are rendered directly from the fixed Excel baseline plus
  // the live delta maintained by statistics-live-calculator.js.
  // This bridge intentionally does not rewrite table cells, preventing
  // duplicate additions or zeroing after polling/realtime refreshes.
})();