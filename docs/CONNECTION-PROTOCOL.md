# پروتکل اتصال Shohada-app به golzarteh.ir

**نسخه پروتکل:** 1.0  
**تاریخ:** 2026-09-21  
**شاخه:** `اتصال`  
**نسخه عملیاتی محافظت‌شده:** `main`

> این فایل «پروتکل زنده اتصال» پروژه است. هر زمان کاربر در ادامه گفتگو بگوید «پروتکل»، منظور همین سند است و اصلاحات موردنظر باید در همین فایل اعمال و نسخه جدید آن ثبت شود.

---

## 1. مرجع و فریز

1. نسخه عملیاتی فعلی پروژه فریز شده است.
2. Commit مرجع فریز:
   `e7f61bd3acc29e20a1ea4a51b73cdd0851bea499`
3. Snapshot:
   `freeze/operational-main-20260921`
4. شاخه توسعه اتصال:
   `اتصال`
5. تغییرات این موضوع فقط در شاخه `اتصال` یا محیط QA انجام شود.
6. هیچ تغییر مستقیمی در `main` مجاز نیست.
7. ورود هر تغییر به `main` فقط بعد از تست و تأیید کاربر انجام شود.

---

## 2. هدف اتصال

هدف، استفاده از اطلاعات تکمیلی `golzarteh.ir` در کنار اطلاعات موجود در Shohada-app است.

اتصال برای تکمیل اطلاعات نتیجه جستجو است، نه جایگزین کردن داده اصلی سامانه.

اطلاعات مهم مورد نظر:

- صفحه شهید
- عکس شهید
- عکس سنگ مزار
- سایر تصاویر
- اطلاعات متنی تکمیلی

---

## 3. قاعده اصلی تطبیق

### اولویت شماره 1
**نام + نام خانوادگی**

نام و نام خانوادگی مبنای اول جستجو و تطبیق هستند.

### اولویت شماره 2
**آدرس مزار**

پس از پیدا شدن نام و نام خانوادگی، آدرس مزار برای:

- تأیید هویت رکورد
- تفکیک افراد همنام
- افزایش اطمینان
- جلوگیری از انتساب اشتباه عکس

استفاده می‌شود.

### قاعده مهم

آدرس مزار نباید معیار اولیه و مستقل جستجوی اتصال باشد، مگر در شرایط خاصی که بعداً در پروتکل تصویب شود.

---

## 4. برآورد شباهت فعلی

این اعداد فعلاً برآورد طراحی هستند و آمار قطعی محسوب نمی‌شوند:

- شباهت اسامی: حدود 95 درصد
- شباهت آدرس مزارها: حدود 80 تا 90 درصد

این برآوردها باید در مرحله آزمایش واقعی با نمونه‌های اندازه‌گیری‌شده اصلاح شوند.

---

## 5. ممنوعیت تطبیق اشتباه

1. صرف شباهت نام برای تطبیق قطعی کافی نیست.
2. عکس یک شهید نباید به رکورد شهید دیگری نسبت داده شود.
3. در حالت مشکوک، نتیجه باید «احتمالی» اعلام شود.
4. در نبود اطمینان کافی، اطلاعات گلزارته نمایش داده نشود.

---

## 6. وضعیت‌های استاندارد تطبیق

هر اتصال باید یکی از این وضعیت‌ها را داشته باشد:

- `قطعی`
- `احتمالی`
- `بدون تطبیق`

و روش تطبیق باید در صورت نیاز قابل ثبت باشد.

---

## 7. ترتیب داده‌های مورد نیاز

### اولویت بسیار بالا

- نام
- نام خانوادگی
- قطعه
- ردیف
- شماره
- شناسه گلزارته
- URL صفحه شهید

### اولویت تصویری

1. عکس سنگ مزار
2. عکس شهید
3. سایر تصاویر

### اولویت تکمیلی

- نام پدر
- تاریخ تولد
- تاریخ شهادت
- محل شهادت
- عملیات
- یگان
- شرح حال
- سایر اطلاعات موجود

---

## 8. معماری رسمی

معماری اتصال باید از این الگو پیروی کند:

```
Search
  ↓
Resolver
  ↓
Index
  ↓
Loader / Connector
  ↓
Golzarteh Data
  ↓
UI
```

وظیفه‌ها باید از هم جدا باشند:

- Search = پیدا کردن رکورد داخلی
- Resolver = تشخیص رکورد متناظر
- Index = نگهداری رابطه
- Loader = دریافت اطلاعات تکمیلی
- UI = نمایش

---

## 9. اصل منبع مرجع

Shohada-app مرجع عملیاتی اطلاعات خودش است.

Golzarteh در این مرحله منبع اطلاعات تکمیلی است.

Golzarteh نباید بدون تصویب جداگانه:

- داده عملیاتی را جایگزین کند
- رکورد Supabase را تغییر دهد
- آمار برنامه را تغییر دهد
- وضعیت مراحل سنگ را تغییر دهد

---

## 10. اصل توسعه ایمن

قبل از هر تغییر کد:

```
Recovery → Diagnose → Minimal Change → Technical Test → Practical Test → Approval
```

هیچ توسعه اتصال نباید باعث شکستن قابلیت‌های فعلی جستجو، نتایج دقیق/مشابه، جزئیات یا بازگشت شود.

---

## 11. اصل داده و Supabase

تا قبل از تصویب مدل اتصال:

- Schema جدید اضافه نشود.
- داده عملیاتی نوشته یا اصلاح نشود.
- Migration اجرا نشود.
- تطبیق انبوه روی جدول اصلی اجرا نشود.

ابتدا معماری و نمونه واقعی باید تأیید شود.

---

## 12. اصل تغییرات پروتکل

هر زمان کاربر گفت **«پروتکل»**:

1. همین فایل مرجع بررسی شود.
2. خواسته جدید کاربر با مفاد قبلی تطبیق داده شود.
3. مفاد جدید جایگزین/تکمیل مفاد قبلی در همین فایل شود.
4. نسخه پروتکل افزایش یابد.
5. تاریخ به‌روزرسانی ثبت شود.
6. هیچ برداشت شفاهی جای سند مکتوب را نگیرد.

---

## 13. وضعیت جاری

در حال حاضر:

- طراحی معماری انجام شده.
- پیاده‌سازی شروع نشده است.
- منبع خارجی هنوز از نظر HTML/API/JSON/AJAX به صورت قطعی استخراج نشده است.
- مرحله بعد فقط بررسی فنی واقعی `golzarteh.ir` و تعیین قرارداد داده است.

**هیچ‌یک از مفاد این سند به معنی تأیید پیاده‌سازی نشده است؛ این سند، پروتکل طراحی و کنترل تغییرات است.**
