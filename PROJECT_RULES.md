# قوانین اجرایی پروژه Shohada-app / GolzarStone

## مرجع واحد اجرای روزانه

این فایل مرجع اجرایی یکپارچه برای توسعه، نگهداری، تست، بررسی و انتشار پروژه است.

دو سند زیر اجزای اصلی و لازم‌الاجرای این مرجع هستند:

- `PROJECT_CHARTER.md` — منشور مادر پروژه و اصول معماری، کسب‌وکار، داده، UX، امنیت، عملکرد، تست و Release.
- `PROJECT_SAFETY_RULES.md` — قوانین ایمنی، Backup/Recovery، حفاظت از کد و داده، تغییرات ماژولار، تست و قانون توقف.

محتوای این دو سند باید همیشه در تمام فعالیت‌های پروژه رعایت شود و در صورت تعارض، اولویت با حفاظت از داده، پایداری سامانه، قابلیت بازگشت و صداقت در تست است.

---

## 1. اصل بنیادین
**اول پایداری، سپس توسعه.**

هیچ قابلیت جدیدی ارزش آن را ندارد که باعث خراب شدن قابلیت‌های قبلی شود. نسخه سالم، کد اصلی و داده واقعی دارایی محافظت‌شده‌اند.

---

## 2. فرآیند اجباری هر کار فنی

**بررسی → خواندن قوانین پروژه → بررسی GitHub → Recovery Point → تشخیص علت واقعی → تعیین محدوده تغییر → تغییر حداقلی و کنترل‌شده → بررسی مجدد → Verify → Test → Review → گزارش → در صورت تأیید Merge/Release**

---

## 3. حفاظت از کد اصلی

- حذف یا بازنویسی گسترده بدون دلیل و تأیید ممنوع است.
- جایگزینی فایل با نسخه ناقص ممنوع است.
- بازسازی فایل بر اساس حدس ممنوع است.
- Refactor پرریسک یا گسترده بدون بررسی اثرات ممنوع است.
- اختلاف غیرعادی حجم یا تعداد خطوط فایل باید باعث توقف و بررسی شود.
- `main` محل آزمایش و خطا نیست.

---

## 4. معماری تغییرات جدید

قابلیت‌های جدید تا حد امکان باید به‌صورت ماژول، افزونه یا فایل مستقل خارج از کد هسته ساخته شوند و از طریق Loader/Index یا نقطه اتصال مشخص به برنامه متصل شوند.

اگر تغییر در هسته ناگزیر باشد، دامنه تغییر باید حداقلی، کنترل‌شده، مستند، قابل بررسی و قابل بازگشت باشد.

---

## 5. Backup و Recovery Point

پیش از هر تغییر مهم، Recovery Point مشخص و قابل بازگشت ثبت شود. در چند تغییر هم‌زمان، یک Recovery Point واحد پیش از شروع ثبت و دامنه همه تغییرات مشخص شود.

---

## 6. Supabase و داده‌های واقعی

بدون اجازه صریح کاربر هیچ INSERT، UPDATE، DELETE یا تغییر در Schema، Policy/RLS، Function، Trigger، Realtime یا داده‌های آماری مجاز نیست.

تست نباید با حذف، ویرایش یا ایجاد داده واقعی کاربران انجام شود.

---

## 7. داده، جست‌وجو و آمار

- Single Source of Truth حفظ شود.
- اعداد مبنای تأییدشده بدون دستور کاربر تغییر نکنند.
- منبع و روش محاسبه آمار مشخص و قابل بازتولید باشد.
- فیلدهای مکانی قطعه، ردیف و شماره در صورت نیاز به‌صورت متن حفظ شوند.
- منطق جست‌وجو و تطبیق باید شفاف، قابل تکرار و قابل توضیح باشد.

---

## 8. تست و صداقت در تست

بعد از هر تغییر، فایل تغییرکرده، وابستگی‌ها، Load Path، Syntax، Runtime، Regression و عملکرد مرتبط بررسی شود و در صورت امکان تست عملی انجام شود.

ایجاد فایل یا ثبت Commit به‌تنهایی موفقیت محسوب نمی‌شود.

تست انجام‌نشده هرگز به‌عنوان تست انجام‌شده گزارش نشود. اگر تست واقعی ممکن نیست، محدودیت، موارد تست‌نشده، اقدام لازم از طرف کاربر و نتیجه مورد انتظار صریحاً اعلام شود.

---

## 9. قانون توقف

در صورت وجود هر یک از موارد زیر، ادامه تغییر متوقف و ابتدا بررسی انجام شود:

- Backup یا Recovery Point نامشخص
- اختلاف غیرعادی کد یا حجم فایل
- فایل ناقص
- Dependency مفقود
- خطر از دست رفتن داده
- نبود امکان تست معتبر
- احتمال آسیب به نسخه سالم

---

## 10. Release

نسخه تحویلی باید دقیقاً همان نسخه تست‌شده باشد. باز شدن برنامه به‌تنهایی معیار Release نیست؛ قابلیت‌ها، Load Path، Dependency، داده، Search، ثبت، ویرایش، تأیید، Statistics و سایر بخش‌های مرتبط باید بررسی شوند.

---

## 11. گزارش پایان مرحله

گزارش هر مرحله باید در صورت مرتبط بودن شامل این موارد باشد:

- مشکلات و علت واقعی
- فایل‌های تغییرکرده
- فایل‌های جدید و حذف‌شده
- تغییرات معماری
- تست‌ها و نتایج واقعی
- موارد باقی‌مانده
- اقدامات موردنیاز کاربر
- Commit و Recovery Point
- درصد پیشرفت واقعی

---

## 12. اصل نهایی

**هیچ تغییری نباید به قیمت از دست رفتن کد، داده، قابلیت یا پایداری نسخه سالم انجام شود.**

کدهای اصلی محافظت شوند؛ داده‌های Supabase محافظت شوند؛ تغییرات قابل ردیابی و بازگشت باشند؛ قابلیت‌های جدید تا حد امکان مستقل باشند؛ تست‌ها واقعی و صادقانه گزارش شوند؛ و هیچ مرحله ناقصی کامل اعلام نشود.

---

## اسناد مرجع

- `PROJECT_CHARTER.md`
- `PROJECT_SAFETY_RULES.md`
- `DEVELOPMENT_WORKFLOW.md`
- `REVIEW.md`
- `CLAUDE.md`

در صورت تعارض، حفاظت از داده، پایداری سامانه، قابلیت بازگشت و صداقت در تست اولویت دارند.
