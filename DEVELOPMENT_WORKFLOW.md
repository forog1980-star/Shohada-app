# روند توسعه پروژه Shohada-app / GolzarStone

## هدف
این سند روند استاندارد و اجباری توسعه، رفع خطا، تست و انتشار پروژه را تعریف می‌کند.

## 1. شروع هر کار
درخواست کاربر ثبت و محدوده آن مشخص شود. سپس `PROJECT_CHARTER.md` و `PROJECT_SAFETY_RULES.md` خوانده شوند و وضعیت فعلی GitHub بررسی شود.

## 2. بررسی اولیه
قبل از تغییر:
- Branch و Commit فعلی مشخص شود.
- آخرین نسخه سالم و Recovery Point مشخص شود.
- فایل‌ها و وابستگی‌های مرتبط بررسی شوند.
- علت واقعی مشکل مشخص شود، نه فقط محل مشاهده آن.
- اثر تغییر بر بخش‌های دیگر بررسی شود.

## 3. تغییر
تغییرات مهم در Branch مستقل انجام شوند. از تغییر مستقیم و آزمایش روی `main` پرهیز شود. فقط محدوده لازم تغییر کند و هیچ فایل اصلی با نسخه ناقص یا حدسی جایگزین نشود.

## 4. بررسی پس از تغییر
فایل تغییرکرده دوباره خوانده شود. حجم و تعداد خطوط با قبل مقایسه شود. فایل‌های وابسته، مسیر Load، نام فایل‌ها و ارجاعات بررسی شوند.

## 5. تست
حداقل این موارد بررسی شوند:
- Syntax
- Runtime
- Dependency
- مسیر بارگذاری
- عملکرد مرتبط
- Regression
- در صورت امکان تست عملی کاربر

تست انجام‌نشده نباید موفق اعلام شود.

## 6. Review
برای تغییرات مهم، Pull Request ساخته شود و پیش از Merge توسط یک بازبین مستقل یا AI Reviewer بررسی شود. اختلاف غیرعادی یا تغییرات خارج از محدوده باید متوقف و بررسی شوند.

## 7. Merge
تا زمانی که Verify، Test و Review کامل نشده‌اند، تغییر وارد `main` نشود. Merge فقط پس از اطمینان از سالم بودن نسخه و امکان بازگشت انجام شود.

## 8. Supabase
در توسعه این پروژه، Supabase محیط عملیاتی است. بدون اجازه صریح کاربر هیچ تغییر داده یا ساختاری انجام نشود. تست مخرب روی داده واقعی ممنوع است.

## 9. انتشار
قبل از Release، اجرای برنامه، فایل‌های لازم، Dependencyها، قابلیت‌های مرتبط، داده، Search، ثبت، ویرایش، تأیید و Statistics در صورت ارتباط بررسی شوند.

## 10. گزارش
گزارش هر مرحله شامل تغییرات، علت، تست‌ها، نتایج، مشکلات باقی‌مانده، Commit و Recovery Point باشد.

## 11. پایان مرحله
مرحله فقط زمانی تمام‌شده اعلام شود که محدوده آن پایدار، تست‌شده و قابل انتشار باشد. اگر محدودیت محیط وجود دارد، صریحاً اعلام شود.

## 12. توسعه افزونه‌ای و حفاظت از هسته
قابلیت جدید تا حد امکان باید خارج از کد هسته و در فایل یا ماژول مستقل ساخته شود. سپس از طریق Loader/Index یا نقطه اتصال مشخص به برنامه متصل شود. این روش اولویت دارد و جایگزین بازنویسی کد اصلی است.

اگر اصلاح کد اصلی اجتناب‌ناپذیر باشد، ابتدا نسخه کامل و سالم آن حفظ شود، Recovery Point ثبت شود و سپس فقط بخش لازم با کمترین دامنه تغییر کند.

## 13. Backup قبل از مجموعه تغییرات
اگر چند اصلاح یا چند گزینه قرار است در یک نوبت انجام شوند، قبل از شروع یک نسخه کامل و قابل بازگشت از برنامه یا مجموعه فایل‌های درگیر ثبت شود. هیچ‌یک از تغییرات نباید قبل از ایجاد این نقطه بازگشت روی نسخه عملیاتی آزمایش شود.

## 14. چرخه استقرار ایمن
**Backup/Recovery Point → توسعه مستقل → اتصال از طریق Loader/Index → Verify → Test → Review → اعمال روی نسخه هدف**

## 15. ممنوعیت جایگزینی ناقص
هیچ فایل اصلی نباید با محتوای جزئی، ناقص، حدسی یا بازسازی‌شده جایگزین شود. قبل و بعد از تغییر، ساختار، اندازه، تعداد خطوط و توابع کلیدی بررسی شوند.

## 16. کنترل چند تغییر
هر اصلاح باید قابل شناسایی و قابل بازگشت باشد. تغییرات خارج از محدوده درخواست کاربر نباید همراه آن وارد شوند.

## روند اجباری
**Request → Rules → GitHub Check → Recovery Point → Diagnosis → Branch → Minimal/Modular Change → Verify → Test → Review → Report → Merge/Release**
