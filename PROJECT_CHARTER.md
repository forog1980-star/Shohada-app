# منشور اصلی پروژه Shohada-app / GolzarStone
## Project Charter

### عنوان پروژه
**Shohada-app / GolzarStone**  
**سامانه مدیریت، پایش و تحلیل وضعیت سنگ مزار شهدای گلزار شهدای تهران**

---

## ۱. ماهیت و مأموریت پروژه
این پروژه صرفاً یک نرم‌افزار جست‌وجو، ثبت اطلاعات یا نمایش آمار نیست. GolzarStone سامانه‌ای تخصصی برای مدیریت، پایش، ثبت، تحلیل و کنترل عملیات مرتبط با سنگ مزار شهدای گلزار شهدای تهران است و باید نیازهای واقعی بهره‌برداری سازمانی را پوشش دهد.

این پروژه از منظر شهری و مدیریتی، بخشی از نظام مدیریت و نگهداری یک فضای شهری ـ یادمانی است؛ بنابراین کیفیت داده، صحت موقعیت مزار، کنترل عملیات، گزارش‌پذیری، قابلیت ردیابی و پایداری سامانه اهمیت حیاتی دارد.

هدف نهایی، ایجاد سامانه‌ای پایدار، دقیق، امن، مقیاس‌پذیر، قابل نگهداری، قابل توسعه، قابل گزارش‌گیری و مناسب بهره‌برداری واقعی است.

## ۲. نقش‌های مورد انتظار
در تمام مراحل توسعه، بررسی و نگهداری پروژه، از دیدگاه‌های زیر به پروژه نگاه شود:
- معمار ارشد نرم‌افزار
- توسعه‌دهنده ارشد Full Stack
- توسعه‌دهنده Frontend و Backend
- مهندس و تحلیلگر داده
- متخصص پایگاه داده و Supabase
- متخصص Web و PWA و در صورت وجود Mobile/Windows
- متخصص امنیت
- متخصص UI/UX
- مهندس QA و تست
- Release Engineer
- متخصص تحلیل و گزارش‌گیری
- متخصص یکپارچه‌سازی و همگام‌سازی داده

## ۳. اصل بنیادین
### اول پایداری، سپس توسعه
هیچ قابلیت جدیدی ارزش آن را ندارد که باعث خراب شدن قابلیت‌های قبلی شود. هر تغییر باید قابل بررسی، قابل بازگشت، دارای دامنه مشخص، همراه با بررسی وابستگی‌ها و تست باشد.

## ۴. حفاظت از کدهای اصلی
کدهای اصلی پروژه دارایی محافظت‌شده محسوب می‌شوند.
- حذف گسترده کد بدون دلیل و تأیید ممنوع است.
- جایگزینی یک فایل کامل با نسخه ناقص ممنوع است.
- بازسازی فایل بر اساس حدس ممنوع است.
- Refactor گسترده بدون بررسی اثرات آن ممنوع است.
- تغییرات باید حداقلی، شفاف و قابل بازگشت باشند؛ مگر بازطراحی اصولی با تأیید قبلی لازم باشد.
- قبل از تغییر، وضعیت قبلی باید قابل بازیابی باشد.
- اختلاف غیرعادی حجم یا تعداد خطوط فایل باید هشدار جدی تلقی شود.

## ۵. GitHub مرجع رسمی کد
GitHub مرجع رسمی نسخه‌بندی و تاریخچه کد پروژه است. قبل از هر تغییر، Branch، Commit، آخرین نسخه سالم، Backup/Recovery Point، فایل‌های مرتبط و تغییرات اخیر بررسی شوند.

نسخه `main` محل آزمایش و خطا نیست. فرآیند ترجیحی:
**Issue → Branch → Recovery Point → Change → Verify → Test → Review → Pull Request → Merge**

## ۶. حفاظت از Supabase و داده‌های عملیاتی
Supabase و داده‌های عملیاتی بخش حیاتی سامانه هستند.

**بدون اجازه صریح کاربر هیچ تغییری در داده یا ساختار Supabase انجام نشود.**

این ممنوعیت شامل INSERT، UPDATE، DELETE، تغییر ساختار جدول، Policy، Function، Trigger، Realtime و داده‌های آماری است. تست فنی نیز نباید روی داده واقعی کاربران انجام شود.

## ۷. اصل Single Source of Truth
برای داده‌های عملیاتی یک منبع معتبر و مشخص وجود داشته باشد. هر عدد آماری باید منبع و روش محاسبه مشخص داشته باشد. اعداد مبنای تأییدشده بدون دستور تغییر داده نشوند.

## ۸. اطلاعات پایه مزار
اطلاعات پایه شامل نام، نام خانوادگی، قطعه، ردیف، شماره، نوع عملیات، مرحله عملیات، وضعیت تأیید، توضیحات، تاریخ ثبت، تاریخ ویرایش و توضیحات ویرایش است.

فیلدهای مکانی مانند قطعه، ردیف و شماره حساس‌اند و در صورت نیاز باید به‌صورت متن نگهداری شوند تا قالب و صفرهای ابتدایی از بین نرود.

## ۹. جست‌وجو و شناسایی
جست‌وجو باید بر اساس هویت و موقعیت واقعی رکوردها و با قواعد شفاف و قابل تکرار انجام شود. موقعیت مزار با ترکیب قطعه، ردیف و شماره کنترل می‌شود و منطق تطبیق مبهم یا غیرقابل توضیح نباید وارد سیستم شود.

## ۱۰. منطق عملیات سنگ مزار
دو مسیر اصلی فعلی:

### ترمیمی
1. طرح آماده ارسال به واحد مرمت
2. سنگ مرمتی آماده
3. نصب سنگ مرمت شده

### تعویضی
1. طرح آماده ارسال به واحد تعویض
2. سنگ تعویضی آماده
3. سنگ تعویضی نصب شده

این مراحل بخشی از Business Logic پروژه هستند و تغییر در معنا، ترتیب یا نام آن‌ها باید با بررسی وابستگی‌ها انجام شود.

## ۱۱. استقلال نمایش از منطق کسب‌وکار
تعریف Stage، Status، Operation و مقادیر مشترک نباید به شکل پراکنده تکرار شود. نمایش کاربر، آمار و منطق داخلی باید تا حد امکان از تعریف مشترک استفاده کنند.

## ۱۲. معماری نرم‌افزار
اصول معماری: Modular Architecture، Separation of Concerns، DRY، Low Coupling، High Cohesion، Maintainability، Testability، Extensibility و Reusability.

Refactor معماری فقط پس از شناسایی مشکل واقعی، بررسی اثرات، تعیین روش بازگشت و ارزیابی ریسک انجام شود.

## ۱۳. Frontend و UX
تمام صفحات باید ساده، روان، خوانا، سریع، یکپارچه و قابل فهم و در محیط‌های هدف سازگار باشند. Error State، Empty State، Loading و Success State باید درست مدیریت شوند.

## ۱۴. نسخه‌ها و PWA
در صورت وجود Web، PWA، Android یا Windows، تا حد امکان Data Model، Backend، Business Logic و قواعد مشترک هماهنگ باقی بمانند.

## ۱۵. آمار و گزارش‌گیری
هر عدد آماری باید منبع مشخص، روش محاسبه مشخص و قابلیت بازتولید داشته باشد. اصلاح فنی نباید مبانی آماری را ناخواسته تغییر دهد.

## ۱۶. Live Data و Realtime
در صورت استفاده از Realtime، رویدادهای INSERT، UPDATE و DELETE باید در صورت نیاز به‌شکل کنترل‌شده اثر خود را در محاسبات و نمایش نشان دهند؛ Realtime نباید باعث تغییر خودکار و غیرقابل کنترل داده شود.

## ۱۷. Cache و نسخه فایل‌ها
Cache نباید مانع دریافت نسخه اصلاح‌شده شود. بعد از تغییر مهم در JS/CSS، Cache Busting بررسی شود؛ اما Cache Busting نباید بهانه‌ای برای تغییر غیرضروری کد اصلی باشد.

## ۱۸. امنیت
Authentication، Authorization، Session، Token، API، Supabase، Policy/RLS، Validation، Sanitization، XSS، Injection، Upload، Storage، CDN Dependencies و Service Worker در تغییرات مرتبط بررسی شوند.

## ۱۹. عملکرد
سرعت بارگذاری، حجم فایل‌ها، تعداد درخواست‌ها، Cache، مصرف حافظه، Runtime، Network، Search و Statistics Performance در تغییرات مرتبط بررسی شوند.

## ۲۰. وابستگی‌ها
Dependencyهای بلااستفاده، تکراری، قدیمی، ناامن یا غیرضروری شناسایی شوند؛ حذف فقط پس از بررسی تمام محل‌های استفاده مجاز است.

## ۲۱. تست
پس از هر تغییر:
1. فایل تغییرکرده بررسی شود.
2. فایل‌های وابسته بررسی شوند.
3. مسیر اجرای تغییر بررسی شود.
4. Syntax و Runtime بررسی شود.
5. Regression بررسی شود.
6. در صورت امکان تست عملی انجام شود.

فقط تست همان فایل کافی نیست.

## ۲۲. تست سطح سامانه
در تغییرات مرتبط، جریان کامل ثبت، ذخیره، بازیابی، جست‌وجو، ویرایش، تأیید، آمار و نمایش بررسی شود.

## ۲۳. صداقت در تست
اگر محیط امکان تست واقعی ندارد، دقیقاً اعلام شود چه چیزی تست نشده، چرا تست نشده، کاربر چه تستی باید انجام دهد و نتیجه مورد انتظار چیست. هیچ تست فرضی به‌عنوان تست واقعی گزارش نشود.

## ۲۴. مدیریت تغییرات
هر درخواست جدید از نظر سازگاری معماری، اثر بر داده، UI، آمار، نسخه‌های دیگر، وابستگی‌ها، امنیت و عملکرد بررسی شود. اگر کاربر صریحاً گفت «فقط این مورد را اصلاح کن»، از تغییرات جانبی غیرضروری خودداری شود.

## ۲۵. Backup و Recovery Point
برای تغییرات مهم Recovery Point مشخص باشد. باید بتوان نسخه قبل، Commit قبل، نسخه جدید و روش بازگشت را مشخص کرد.

## ۲۶. Release
باز شدن برنامه به‌تنهایی معیار Release نیست. وجود فایل‌ها، سلامت Dependencyها، Load Path، داده، Search، ثبت، ویرایش، تأیید، Statistics و سایر قابلیت‌های مرتبط باید بررسی شود.

## ۲۷. توقف در شرایط خطرناک
اگر نسخه موجود خراب، Backup نامشخص، اختلاف کد غیرعادی، فایل ناقص، Dependency مفقود، خطر از دست رفتن داده یا نبود امکان تست وجود دارد، ابتدا توقف و بررسی انجام شود.

## ۲۸. قانون طلایی اجرای روزانه
هر درخواست فنی باید با این ترتیب انجام شود:

**بررسی → خواندن قوانین پروژه → بررسی GitHub → Recovery Point → تشخیص علت واقعی → تعیین محدوده تغییر → تغییر حداقلی و کنترل‌شده → بررسی مجدد → تست → بازبینی → گزارش → در صورت تأیید Merge/Release**

## ۲۹. معیار تکمیل
یک بخش زمانی تکمیل‌شده است که عملکرد آن کامل، پایدار، تست‌شده و با بخش‌های وابسته سازگار باشد و داده‌های موجود را خراب نکرده باشد.

## ۳۰. گزارش پایان مرحله
گزارش باید شامل مشکلات، علت‌ها، فایل‌های تغییرکرده، فایل‌های جدید و حذف‌شده، تغییرات معماری، تست‌ها و نتایج، موارد باقی‌مانده، اقدامات موردنیاز کاربر، Commit/Recovery Point و درصد پیشرفت واقعی باشد.

## ۳۱. تحویل نسخه
نسخه تحویلی باید دقیقاً همان نسخه تست‌شده باشد. در صورت نیاز به تحویل کامل، کل پروژه به‌صورت یکپارچه و قابل بازیابی تحویل شود.

## ۳۲. اصل نهایی
هیچ تغییری نباید به قیمت از دست رفتن کد، داده، قابلیت یا پایداری نسخه سالم موجود انجام شود.

کدهای اصلی محافظت شوند؛ داده‌های Supabase محافظت شوند؛ تغییرات قابل ردیابی و بازگشت باشند؛ تست‌ها واقعی و صادقانه گزارش شوند؛ و هیچ مرحله ناقصی کامل اعلام نشود.

## ۳۳. رابطه این سند با سایر اسناد
این فایل **منشور مادر و مرجع اجرایی پروژه** است. اسناد `PROJECT_SAFETY_RULES.md`، `DEVELOPMENT_WORKFLOW.md`، `REVIEW.md` و `CLAUDE.md` باید با آن سازگار باشند. در تعارض‌ها، حفاظت از داده، پایداری سامانه، قابلیت بازگشت و صداقت در تست در اولویت است.
