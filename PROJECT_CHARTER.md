# منشور اصلی پروژه Shohada-app / GolzarStone
## Project Charter

این سند، منشور مادر و سند اجرایی پروژه است و هدف آن حفظ پایداری، امنیت، کیفیت داده و قابلیت توسعه سامانه مدیریت و پایش بهسازی سنگ‌های شهدای گلزار شهدای تهران است.

## ۱. مأموریت
GolzarStone سامانه‌ای تخصصی برای ثبت، جست‌وجو، پایش، مدیریت، تحلیل و گزارش‌گیری عملیات مرتبط با سنگ مزار شهدای گلزار شهدای تهران است. هدف نهایی، سامانه‌ای پایدار، دقیق، امن، قابل نگهداری، مقیاس‌پذیر و مناسب بهره‌برداری واقعی است.

## ۲. اصل بنیادین
کیفیت و پایداری بر سرعت مقدم است. هیچ تغییر جدیدی نباید قابلیت سالم قبلی، کد موجود یا داده عملیاتی را بدون دلیل و کنترل از بین ببرد.

## ۳. حفاظت از کد
کدهای اصلی دارایی محافظت‌شده‌اند. حذف یا بازنویسی گسترده، جایگزینی فایل با نسخه ناقص، بازسازی حدسی و Refactor پرریسک ممنوع است. تغییرات باید کنترل‌شده، حداقلی و قابل بازگشت باشند؛ بازطراحی گسترده فقط پس از بررسی و تأیید قبلی مجاز است. اختلاف غیرعادی در حجم یا تعداد خطوط فایل هشدار بحرانی محسوب می‌شود.

## ۴. GitHub
GitHub مرجع رسمی نسخه‌بندی و تاریخچه کد است. قبل از هر تغییر Branch، Commit، آخرین نسخه سالم، Recovery Point، فایل‌های مرتبط، وابستگی‌ها و تغییرات اخیر بررسی شوند. `main` محل آزمایش نیست. فرآیند ترجیحی:
**Issue → Branch → Recovery Point → Change → Verify → Test → Review → Pull Request → Merge**

## ۵. Supabase و داده
بدون اجازه صریح کاربر هیچ تغییر داده یا ساختاری در Supabase انجام نشود. این ممنوعیت شامل INSERT، UPDATE، DELETE، Schema، Policy/RLS، Function، Trigger، Realtime و داده‌های آماری است. تست روی داده واقعی ممنوع است مگر با دستور صریح و روش امن.

## ۶. اطلاعات مزار
اطلاعات پایه پروژه شامل نام، نام خانوادگی، قطعه، ردیف، شماره، نوع عملیات، مرحله عملیات، وضعیت تأیید، توضیحات و اطلاعات ثبت/ویرایش است. قطعه، ردیف و شماره داده‌های مکانی حساس‌اند و در صورت نیاز به‌صورت متن نگهداری شوند.

## ۷. جست‌وجو
جست‌وجو باید شفاف، قابل تکرار و مبتنی بر هویت و موقعیت واقعی رکورد باشد. ترکیب قطعه، ردیف و شماره برای محل مزار اهمیت دارد و منطق تطبیق مبهم یا غیرقابل توضیح نباید وارد سیستم شود.

## ۸. مراحل عملیات
### ترمیمی
1. طرح آماده ارسال به واحد مرمت
2. سنگ مرمتی آماده
3. نصب سنگ مرمت شده

### تعویضی
1. طرح آماده ارسال به واحد تعویض
2. سنگ تعویضی آماده
3. سنگ تعویضی نصب شده

Stage و Status بخشی از Business Logic هستند و تغییر آن‌ها باید با بررسی تمام وابستگی‌ها انجام شود.

## ۹. معماری و کیفیت کد
اصول پروژه شامل Modular Architecture، Separation of Concerns، DRY، Low Coupling، High Cohesion، Maintainability، Testability، Extensibility و Reusability است. منطق مشترک تا حد امکان یک منبع مرکزی داشته باشد.

## ۱۰. Frontend، PWA و نسخه‌ها
Web، PWA و هر نسخه Mobile/Windows در صورت وجود باید تا حد امکان از Data Model، Backend و Business Logic هماهنگ استفاده کنند. UX باید ساده، سریع، خوانا، یکپارچه و مناسب محیط‌های هدف باشد.

## ۱۱. آمار و Realtime
هر عدد آماری باید منبع و روش محاسبه مشخص داشته باشد. اعداد مبنای تأییدشده بدون دستور تغییر نکنند. Realtime نباید موجب تغییر خودکار و غیرقابل کنترل داده شود.

## ۱۲. امنیت
در تغییرات مرتبط، Authentication، Authorization، Session، Token، API، RLS/Policy، Validation، Sanitization، XSS، Injection، Upload، Storage، CDN و Service Worker بررسی شوند.

## ۱۳. عملکرد و وابستگی‌ها
در تغییرات مرتبط، سرعت بارگذاری، حجم فایل، Network، Cache، Runtime، Search و Statistics بررسی شوند. Dependencyهای بلااستفاده، تکراری، قدیمی یا ناامن فقط پس از بررسی محل‌های استفاده حذف شوند.

## ۱۴. تست
پس از هر تغییر، فایل تغییرکرده، وابستگی‌ها، مسیر Load، Syntax، Runtime و Regression بررسی شوند. در صورت امکان تست عملی سطح سامانه انجام شود. تست فرضی نباید به‌عنوان تست واقعی گزارش شود.

## ۱۵. مدیریت تغییر
اگر کاربر گفت «فقط این مورد را اصلاح کن»، تغییر جانبی غیرضروری انجام نشود. هر تغییر جدید باید از نظر اثر بر معماری، داده، UI، آمار، نسخه‌ها، امنیت و عملکرد بررسی شود.

## ۱۶. توقف در شرایط خطرناک
اگر Backup نامشخص، اختلاف کد غیرعادی، فایل ناقص، Dependency مفقود، احتمال از دست رفتن داده یا نبود امکان تست وجود دارد، ابتدا کار متوقف و وضعیت بررسی شود.

## ۱۷. قانون اجرای روزانه
**بررسی → خواندن منشور و قوانین → بررسی GitHub → Recovery Point → تشخیص علت → تعیین محدوده → تغییر حداقلی → بررسی مجدد → تست → Review → گزارش → در صورت تأیید Merge/Release**

## ۱۸. گزارش پایان مرحله
گزارش باید شامل مشکلات، علت‌ها، فایل‌های تغییرکرده، فایل‌های جدید/حذف‌شده، تغییرات معماری، تست‌ها و نتایج، موارد باقی‌مانده، اقدامات کاربر، Commit/Recovery Point و درصد پیشرفت واقعی باشد.

## ۱۹. معیار تکمیل و Release
بخش فقط زمانی تکمیل‌شده محسوب می‌شود که پایدار، تست‌شده، سازگار و بدون مشکل بحرانی باشد. باز شدن برنامه به‌تنهایی معیار Release نیست.

## ۲۰. اصل نهایی
هیچ تغییری نباید به قیمت از دست رفتن کد، داده، قابلیت یا پایداری نسخه سالم انجام شود. حفاظت از داده، قابلیت بازگشت و صداقت در تست همیشه مقدم است.
