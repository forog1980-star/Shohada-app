# قوانین ایمنی پروژه Shohada-app / GolzarStone

## 1. اصل حفاظت
کدهای اصلی، داده‌های عملیاتی و نسخه سالم موجود دارایی محافظت‌شده‌اند. پایداری بر سرعت مقدم است.

## 2. قبل از هر تغییر
وضعیت GitHub، شاخه، Commit، آخرین نسخه سالم، Recovery Point، فایل‌های مرتبط، وابستگی‌ها و اثر تغییر بررسی شود.

## 3. کدهای اصلی
حذف یا بازنویسی گسترده، جایگزینی فایل با نسخه ناقص، بازسازی حدسی و Refactor پرریسک ممنوع است. تغییرات باید حداقلی، کنترل‌شده، قابل بررسی و قابل بازگشت باشند.

## 4. هشدار تغییر غیرعادی
کاهش غیرعادی حجم یا تعداد خطوط هر فایل، حذف گسترده توابع یا تغییر تعداد زیاد فایل‌ها باید قبل از ادامه متوقف و بررسی شود.

## 5. نسخه اصلی
`main` محل آزمایش و خطا نیست. تغییرات مهم ابتدا در Branch مستقل انجام شوند و پس از Verify، Test و Review وارد نسخه اصلی شوند.

## 6. Supabase
بدون اجازه صریح کاربر هیچ INSERT، UPDATE، DELETE یا تغییر در Schema، Policy/RLS، Function، Trigger، Realtime یا داده‌های آماری مجاز نیست.

## 7. داده‌های واقعی
تست نباید با حذف، ویرایش یا ایجاد داده واقعی انجام شود. در صورت نیاز به تست داده‌ای، روش امن و جداگانه تعیین شود.

## 8. اطلاعات مزار
اطلاعات پایه شامل نام، نام خانوادگی، قطعه، ردیف، شماره، نوع عملیات، مرحله عملیات، وضعیت تأیید، توضیحات و تاریخ‌های ثبت/ویرایش است. قطعه، ردیف و شماره در صورت نیاز به‌صورت متنی حفظ شوند.

## 9. آمار
منبع، مبنای محاسبه و منطق هر عدد مشخص باشد. اعداد مبنای تأییدشده بدون دستور کاربر تغییر نکنند.

## 10. تست
بعد از هر تغییر، فایل تغییرکرده، وابستگی‌ها، مسیر Load، Syntax، Runtime و Regression بررسی شوند. در صورت امکان تست عملی نیز انجام شود.

## 11. صداقت
تست انجام‌نشده نباید انجام‌شده گزارش شود. محدودیت محیط و اقدام لازم از طرف کاربر صریحاً اعلام شود.

## 12. فرآیند اجباری
**بررسی → Recovery Point → تشخیص علت → تغییر حداقلی → بررسی مجدد → تست → Review → گزارش → Merge/Release**

## 13. قانون توقف
اگر Backup نامشخص، اختلاف کد غیرعادی، فایل ناقص، Dependency مفقود، خطر از دست رفتن داده یا نبود امکان تست وجود دارد، ابتدا توقف و بررسی انجام شود.

## 14. قانون نهایی
هیچ اصلاحی نباید باعث از دست رفتن کد، داده، قابلیت یا پایداری نسخه سالم شود. هر تغییر باید قابل ردیابی و قابل بازگشت باشد.

## 15. قانون معماری تغییرات جدید
قابلیت‌های جدید تا حد امکان نباید با بازنویسی کد اصلی ساخته شوند. ابتدا به‌صورت ماژول یا فایل مستقل در خارج از کد هسته ایجاد شوند و سپس از طریق Loader/Index یا نقطه اتصال مشخص به برنامه متصل شوند.

اگر قابلیت جدید ناگزیر به تغییر کد اصلی باشد، این تغییر باید با حداقل دامنه و پس از بررسی وابستگی‌ها انجام شود.

## 16. Backup قبل از تغییر
پیش از هر تغییر در کد اصلی یا هر مجموعه تغییر مرتبط، یک Recovery Point مشخص و قابل بازگشت ثبت شود. اگر چند اصلاح قرار است هم‌زمان انجام شوند، نسخه کامل برنامه یا حداقل مجموعه کامل فایل‌های درگیر باید قبل از اعمال تغییر حفظ شود.

## 17. چرخه ایمن اعمال تغییر
هیچ تغییر جدیدی مستقیماً روی نسخه عملیاتی آزمایش نشود. ترتیب اجباری:
**Backup/Recovery Point → ساخت تغییر در فایل یا ماژول مستقل → اتصال از طریق Loader/Index → Verify → Test → Review → اعمال روی نسخه هدف**

## 18. ممنوعیت جایگزینی ناقص
در صورت نیاز به اصلاح یک فایل اصلی، کل فایل فقط زمانی جایگزین شود که نسخه کامل و سالم آن در دسترس باشد و قبل و بعد از تغییر از نظر حجم، ساختار و وابستگی بررسی شده باشد. هرگز فایل اصلی با محتوای جزئی، ناقص یا بازسازی‌شده جایگزین نشود.

## 19. چند تغییر هم‌زمان
اگر قرار است چند گزینه یا چند اصلاح در یک نوبت انجام شوند، تغییرات باید فهرست و دامنه‌بندی شوند و یک Recovery Point واحد پیش از شروع ثبت شود. هر تغییر باید قابل شناسایی و قابل بازگشت باشد.

## 20. تست پیش از استقرار
نسخه آزمایشی باید قبل از انتقال به نسخه اصلی از نظر Syntax، Load Path، Dependency، Runtime، Regression و عملکرد مرتبط بررسی شود. «ایجاد فایل» یا «ثبت Commit» به‌تنهایی به معنی موفقیت تغییر نیست.

## 21. اصل جداسازی
برای جلوگیری از آسیب به هسته برنامه، قابلیت‌های جدید ترجیحاً به شکل افزونه/ماژول مستقل طراحی شوند. کد هسته فقط در صورت نیاز واقعی و پس از تأیید فرآیند تغییر، اصلاح شود.
